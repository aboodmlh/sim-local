requests==2.32.3
qrcode[pil]==8.2

# Railway start command: python main.py

# Required Railway Variables:
# TELEGRAM_BOT_TOKEN=...
# TELEGRAM_CHAT_ID=...
# Optional: MAX_WORKERS=5, VOUCHER_CODE=EXPERTAFRICA, SKU=ESFX-1681-SL1
# Optional: DELAY_BETWEEN_SUBMISSIONS=0, POLL_MAX_WAIT=120, POLL_INTERVAL=3
# Optional: EMAILS_FILE=emails.txt

# Put one email per line in emails.txt
# The worker performs up to MAX_WORKERS purchases at the same time.

# Important: configure Railway to run this as a one-off/job process or otherwise prevent
# accidental repeated purchases after the process exits.

